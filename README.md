# sync-notepad

sync-notepad
the concept is just syncing notepad as the name suggest we have a database , and whenever i type in phone, it should also appear in my PC
----------- --- 
(flet) 
(watchdog) 
(networking/transport)

the format is abit weird idfk
